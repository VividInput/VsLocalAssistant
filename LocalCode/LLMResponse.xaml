<UserControl x:Class="LocalCode.LLMResponse"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mdxam="clr-namespace:MdXaml;assembly=MdXaml"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="title"/>
        <TextBlock Grid.Row="1" Margin="15,10,0,10" TextWrapping="Wrap" x:Name="message"/>
        <!--
        <WebBrowser Grid.Row="1" x:Name="markdownFrame"/>
        <mdxam:MarkdownScrollViewer x:Name="Markdownview" ScrollViewer.VerticalScrollBarVisibility="Auto" IsSelectionEnabled="True" IsInactiveSelectionHighlightEnabled="True" />
-->
    </Grid>
</UserControl>
